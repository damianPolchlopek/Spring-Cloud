spring:
  application:
    name: PhotoAppAPIConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/damianPolchlopek/PhotoAppConfiguration
#          username:
#          password:
          clone-on-start: true
          default-label: main

server:
  port: 8012

