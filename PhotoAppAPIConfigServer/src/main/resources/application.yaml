spring:
  application:
    name: account-ws

  cloud:
    config:
      server:
        git:
          uri: https://github.com/damianPolchlopek/PhotoAppConfiguration
#          username:
#          password:
          clone-on-start: true
          default-label: main
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8012


management:
  endpoints:
    web:
      exposure:
        include: busrefresh, health
